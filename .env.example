# Supabase — paste these into a local .env.local (which is gitignored).
# Both are safe to expose in client code; the anon key is gated server-side
# by Row Level Security policies on the recaps table.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
